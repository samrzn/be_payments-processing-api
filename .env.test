NODE_ENV=test

HOST=0.0.0.0
PORT=3333
APP_KEY=test-app-key-123456789
APP_URL=http://localhost:3333
LOG_LEVEL=error

SESSION_DRIVER=memory

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=api_user
DB_PASSWORD=api_password
# O padrão correto é nome do banco de dados para testes diferente do banco de desenvolvimento para evitar conflitos e garantir isolamento dos dados, mas não foi aplicado aqui por ser um código apenas de desenvolvimento e não produto que realmente será lançado em PRODUÇÃO.
DB_DATABASE=payments_db
